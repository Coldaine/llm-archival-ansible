---
- name: Master Infrastructure Deployment
  hosts: all
  become: false

  roles:
    - bitwarden-setup
    - claude_unified
    - mcp_servers
    - llm_archival
    - { role: tailscale, when: enable_tailscale | default(false) }
    - { role: cloudflare_tunnel, when: enable_cloudflare_tunnel | default(false) }
